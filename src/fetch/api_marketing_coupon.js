import * as fetch from "./fetch"

export default {
    /*************方法命名规则：cp_ 前缀 cp_ 前缀 cp_ 前缀   写注释 写注释***************/
      /**
        * 提现超上限列表接口
        * @param params
        * @returns {*}
      */
      // cp_trade_audit_list(params) {
      //   return fetch.fetch('wuliu-web-user/emp/trade/tradeAuditList.action', params);
      // }
}
